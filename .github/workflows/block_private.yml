name: block-private
on:
  pull_request
jobs:
  check-for-private-code:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check for any private files
        run: for f in *private* do exit 1; done
        shell: bash
